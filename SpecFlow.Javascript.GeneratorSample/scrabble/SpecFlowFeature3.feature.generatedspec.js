(function(){
	featureSteps('SpecFlowFeature1: In order to avoid silly mistakes\n'+
'As a math idiot\n'+
'I want to be told the sum of two numbers')
	.given('I have entered (.*) into the calculator', function(p0/* Int32 */) {
		/* Set test logic here */
	})
	.when('I press add', function() {
		/* Set test logic here */
	})
	.when('I do truc', function() {
		/* Set test logic here */
	})
	.then('the result should be (.*) on the screen', function(p0/* Int32 */) {
		/* Set test logic here */
	});
})();
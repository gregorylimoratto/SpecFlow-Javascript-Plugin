(function(){
	featureSteps('Préciser la tranche de revenu, US01-Sélectionner la tranche revenu du client: En tant que Distributeur,\n'+
'Je dois saisir l\'estimation des revenus mensuels nets de mon client,\n'+
'Afin d\'être conforme à la loi sur la Lutte Contre le Blanchiment (LCB-FT).')
	.given('Je suis sur l\'écran "(.*)", "(.*)", "(.*)"', function(p0/* String */,p1/* String */,p2/* String */) {
		/* Set test logic here */
	})
	.when('Je sélectionne la tranche de revenus', function() {
		/* Set test logic here */
	})
	.then('Je dispose de deux champs', function(table /* json */) {
		/* Set test logic here */
	})
	.then('La liste des revenus apparaît dans l\'ordre suivant', function(table /* json */) {
		/* Set test logic here */
	})
	.when('Je renseigne les valeurs suivantes', function(table /* json */) {
		/* Set test logic here */
	})
	.then('La saisie est possible', function() {
		/* Set test logic here */
	})
	.then('Le message bloquant apparaît', function(table /* json */) {
		/* Set test logic here */
	})
	.then('Les messages d\'(.*)\'erreur. La fermeture du message se fait à l\'(.*)\'utilisateur', function(p0/* String */,p1/* String */) {
		/* Set test logic here */
	})
	.when('je regarde le code de la page', function() {
		/* Set test logic here */
	})
	.then('je constate que les id de champs sont bien conformes au tableau suivant :', function(table /* json */) {
		/* Set test logic here */
	});
})();